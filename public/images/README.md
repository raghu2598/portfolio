Replace this folder's `profile.jpg` with your own profile image.

Requirements:
- File name must be exactly `profile.jpg`
- Recommended: square image, at least 500x500px
- The layout automatically crops it to a circle using `object-fit: cover`

If `profile.jpg` is missing, the Hero section automatically falls back to a
placeholder with your initials, so the site never breaks.
